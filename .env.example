# Apify API token for scraping AliExpress and Alibaba
# Without this, the app runs in Demo Mode with mock data
APIFY_API_TOKEN=

# Upstash Redis for rate limiting in production (optional)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
